/**
 * Future: inbound email (SendGrid Inbound Parse / CloudMailin / SES).
 * Normalize to IngestionPayload and create Document + ProcessingJob.
 */
export async function handleInboundEmailStub(): Promise<never> {
  throw new Error("Email ingestion is not implemented in MVP.");
}
